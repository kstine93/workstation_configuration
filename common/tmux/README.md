# README - tmux

tmux is a terminal multiplexer -- which basically means that it helps manage multiple terminal sessions together.
In this directory are some configurations for tmux. In the custom_scripts repo there is also a shortcut defined for setting up a custom 3-windowed tmux-managed terminal session.

